{% if app.user %}

    <div class="d-flex justify-content-between p-4 border-bottom border-danger">
        <p class="h1 d-flex text-light">Examen Symfony</p>

        <div class="col d-flex justify-content-end">
            <a class="btn btn-secondary m-2" href="{{ path('app_user_index') }}">Accueil</a>
            {% if app.user.hasRole('ROLE_RH') %}
                <a class="btn btn-secondary m-2" href="{{ path('app_user_new') }}">Ajouter un utlisateur</a>
            {% endif %}
            <a class="btn btn-primary m-2" href="{{ path('app_logout') }}">Se déconnecter</a>
        </div>
    </div>

{% endif %}